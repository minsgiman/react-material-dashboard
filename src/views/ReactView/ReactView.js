import React from 'react';
import TableView from './../../layouts/TableView';
import { react } from './../../markdown/map';

const ReactView = () => {
  return (
    <TableView title="React" items={react}></TableView>
  );
};

export default ReactView;
